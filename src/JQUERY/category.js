$(document).ready(function (){    $(document).on('submit','#category_modal',function (event){        event.preventDefault()        var Data = new FormData(this)        Data.append('action','store')        $.ajax({            url: '../controllers/categoryConroller.php',            type: 'post',            data: Data,            processData: false,            contentType: false,            dataType: 'json',            success: function (res){                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success"                    });                    setTimeout(() =>{window.location.reload()},1000)                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "error"                    });                }            }        })    })    // for showing the category on select    $.ajax({        url: '../controllers/categoryConroller.php',        type: 'post',        data: {action: 'showToSelect'},        success: function (category){            $('#categoryName').append(category)        }    })    // show modal to update category    $(document).on('click','#btn_updateCategory',function (id){         console.log(id.target.value)        $.ajax({            url: '../controllers/categoryConroller.php',            type: 'post',            data: {id: id.target.value, action: 'selectById'},            success: function (category){                $('body').append(category)                $(`#category_modal_${id.target.value}`).modal('show')            }        })    })   // Save Updated Category    $(document).on('submit','.updateCategoryForm',function (event){        event.preventDefault()        var Data = new FormData(this)        Data.append('action','update')        $.ajax({            url: '../controllers/categoryConroller.php',            type: 'post',            data: Data,            processData: false,            contentType: false,            dataType: 'json',            success: function (res){                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success"                    });                    setTimeout(() =>{window.location.reload()},1000)                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "error"                    });                }            }        })    })    // Delete Category    $(document).on('click','#btn_ArchiveCategory',function (id){        Swal.fire({            title: 'Are you sure?',            text: "You want to delete this category!",            icon: 'warning',            showCancelButton: true,            confirmButtonColor: '#3085d6',            cancelButtonColor: '#d33',            confirmButtonText: 'Yes, Delete!'        }).then((result) => {            if (result.isConfirmed) {                $.ajax({                    url: '../controllers/categoryConroller.php',                    type: 'post',                    data: {id: id.target.value, action: 'archive'},                    dataType: 'json',                    success: function (res){                        if (res.success === true){                            Swal.fire({                                text: res.message,                                icon: "success"                            });                            setTimeout(() =>{window.location.reload()},1000)                        }                        if (res.success === false){                            Swal.fire({                                text: res.message,                                icon: "error"                            });                        }                    }                })            }        })    })    })